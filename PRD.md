# Math Tutor — PRD (Product Requirements Document)

> **버전**: 0.1 (MVP 정의)
> **작성일**: 2026-05-08
> **상태**: Draft

---

## 1. 개요

초등학교 1-2학년 남자아이를 대상으로 한 **수학 연산 학습 웹 앱**. 반복 학습의 지루함을 게이미피케이션(RPG/우주 탐험 등 회전 가능한 테마)으로 극복하고, 부모가 자녀의 학습 진도를 파악할 수 있는 구조를 갖춘다.

MVP는 **곱셈, 두자리수 덧셈, 두자리수 뺄셈** 세 가지 연산에 집중하며, 이후 분수·시계·도형·문장제 등으로 점진적 확장한다.

---

## 2. 목표 및 비전

### 2.1 제품 비전
> "아이가 매일 10분 스스로 열어서 즐기는 수학 앱"

### 2.2 핵심 목표 (MVP)
1. **즐거운 반복 학습** — 연산 드릴이 게임처럼 느껴지도록 설계
2. **지속성 있는 동기 부여** — XP, 레벨, 수집 요소로 매일 돌아오게 만들기
3. **확장 가능한 아키텍처** — 새 연산/모드를 모듈로 추가할 수 있는 구조
4. **부모 가시성** — 학습 시간, 정답률, 약점 파악

### 2.3 비목표 (Non-Goals)
- 멀티플레이어 / 실시간 대전 (향후 검토)
- 사회적 공유, 랭킹 (어린 사용자 보호)
- 결제 / 인앱 구매
- 모바일 네이티브 앱 (PWA로 충분)

---

## 3. 타겟 사용자

### 3.1 주 사용자 (Primary)
- **초등 1-2학년 남자아이**
  - 한글 읽기 가능, 영어 단어도 익숙한 편
  - 집중 시간 짧음 (5–15분)
  - 즉각적 피드백·시각 자극에 반응
  - 마우스/터치 모두 사용 (태블릿, 데스크탑)

### 3.2 부 사용자 (Secondary)
- **부모 (사용자 본인)**
  - 진도 확인, 난이도 조정, 사용 시간 관리

### 3.3 핵심 사용 시나리오
1. 아이가 학교에서 돌아와 태블릿/노트북으로 앱을 연다
2. 오늘의 미션(예: "행성 3개 정복")을 본다
3. 곱셈 문제 10개를 풀며 몬스터/외계인을 처치
4. 별/코인 획득, 캐릭터 레벨업, 새 장비/우주선 부품 잠금 해제
5. 부모는 별도 모드에서 어제 정답률, 취약 구구단 등을 확인

---

## 4. MVP 기능 요구사항

### 4.1 학습 콘텐츠

#### F1. 곱셈 (구구단 기반)
- **단 선택**: 2단 ~ 9단, 임의 단 / 혼합 모드
- **출제 방식**:
  - 정방향: `3 × 4 = ?`
  - 역방향: `? × 4 = 12` (난이도 ↑에서)
- **난이도 단계**: Easy(1단·5단) → Normal(2~5단) → Hard(2~9단 혼합) → Boss(역방향 포함)

#### F2. 두자리수 덧셈
- **범위**: 10~99 ± 10~99
- **난이도 단계**:
  - Easy: 받아올림 없음 (예: 23+15)
  - Normal: 받아올림 1회 (예: 27+15)
  - Hard: 받아올림 + 큰 합 (예: 58+47)
- **세로셈 보조 모드**(옵션): 자릿수 정렬 시각화

#### F3. 두자리수 뺄셈
- **범위**: 10~99 - 0~99 (결과 ≥ 0)
- **난이도 단계**:
  - Easy: 두자리수 - 한자리수
  - Normal: 받아내림 없음~1회
  - Hard: 받아내림 + 0이 포함된 자릿수

### 4.2 문제 풀이 UX

| 항목 | 요구사항 |
|------|----------|
| 입력 방식 | (1) 화면 숫자 키패드 (2) 객관식 4지선다 (모드 선택 가능) |
| 시간 제한 | 옵션 — 기본은 무제한, 게임 모드에서만 카운트다운 |
| 피드백 | 정답: 효과음 + 폭죽/이펙트, 오답: 부드러운 진동 + 정답 표시 |
| 힌트 | 오답 2회 시 힌트 자동 노출 (예: 곱셈 → 덧셈으로 풀어보기) |
| 실수 복구 | "다시 풀기" 버튼, 오답 노트 자동 누적 |

### 4.3 게이미피케이션

#### G1. 진행 시스템
- **XP**: 문제당 10 XP, 난이도 배수 (Easy 1x / Normal 1.5x / Hard 2x / Boss 3x)
- **레벨**: XP 누적으로 캐릭터 레벨 상승, 레벨 곡선은 완만하게 (1→2 빠르게, 후반 천천히)
- **연속 정답 콤보**: 3·5·10 콤보 보너스 XP

#### G2. 테마 시스템 (회전 가능)
초기 2가지 테마, 사용자가 **언제든 전환** 가능. 테마는 **시각/사운드/스토리** 레이어를 교체하지만 학습 콘텐츠는 동일.

| 테마 | 컨셉 | 진행 메타포 |
|------|------|-------------|
| **🚀 우주 탐험** | 행성에서 행성으로 이동, 외계 생명체와 만남 | 행성 정복, 우주선 부품 수집, 별 지도 채우기 |
| **⚔️ 몬스터 헌터 (RPG)** | 던전 탐험, 몬스터 처치 | 몬스터 도감, 장비 획득, 보스 레이드 |

**확장 가능 테마(향후)**: 공룡 탐사대, 레이싱 챔피언, 닌자/사무라이, 해적, 과학자 실험실 등.

**테마 회전(Anti-Boredom) 옵션**:
- (a) **사용자 선택**: 시작 화면에서 자유 전환
- (b) **주간 회전**: 매주 메인 테마가 자동 변경 + 보상 보너스 (싫으면 고정 가능)
- (c) **이벤트**: 특정 미션은 다른 테마로 진입 (예: "이번 주는 공룡 발굴 특별편")

기본값은 (a) + (c). 부모/아이가 (b)를 켤 수 있음.

#### G3. 일일 미션 (Daily Quest)
매일 갱신되는 3개 미션. 예:
- "곱셈 문제 20개 풀기"
- "콤보 5회 달성하기"
- "받아올림 덧셈 5문제 정답"
- 완료 시 보너스 보상 (별, 캐릭터 카드, 코인 등)

#### G4. 수집 요소 (Collection)
- **카드/도감**: 정복한 행성, 처치한 몬스터, 발굴한 공룡 등
- **장비/꾸미기**: 캐릭터(또는 우주선) 커스터마이징 — 모자, 갑옷, 색상, 페인팅
- **희귀도**: Common → Rare → Epic → Legendary

#### G5. 보스전 (Boss Battle)
- 일정 진도(예: 한 단원 80% 정답률) 달성 시 잠금 해제
- 10문제를 시간 안에 풀어야 함, 클리어 시 특별 보상
- **틀려도 게임 오버 아님** — 좌절 방지 (체력 시스템은 부드럽게)

### 4.4 학습 데이터 / 적응형 난이도

#### A1. 약점 추적
- 문제별 (예: `7×8`) 정답률, 평균 풀이 시간 기록
- 정답률 < 70% 또는 평균 시간이 평균보다 느린 항목을 "약점"으로 분류
- 약점은 향후 출제에서 가중치 ↑

#### A2. 적응형 출제
- 단순 랜덤이 아닌 **약점 강화 + 신선도 균형** 알고리즘
- 너무 같은 문제만 반복되지 않도록 cooldown

### 4.5 부모 모드 (Parent Mode)
- **PIN 잠금** (4자리)으로 보호된 별도 화면
- 표시 정보:
  - 누적 학습 시간, 일별 추이
  - 영역별(곱셈/덧셈/뺄셈) 정답률
  - 약점 Top 5 (예: `8×7`, `45+38`)
  - 최근 세션 로그
- 설정:
  - 기본 난이도 조정
  - 일일 학습 시간 한도 (옵션)
  - 테마 회전 on/off

### 4.6 다국어 (한국어 / 영어 토글)
- **한국어 기본**, 우측 상단 토글로 영어 전환
- 모든 UI 문자열, 효과음 멘트, 미션 텍스트 i18n 처리
- 숫자/연산 자체는 동일 (학습 효과 + 영어 노출 부수 효과)
- i18n 라이브러리: `next-intl` 권장

### 4.7 접근성 & UX 원칙 (저학년 특화)
- **큰 버튼, 큰 글자** (최소 18px 본문, 24px+ 버튼)
- **색맹 친화** 색상 + 아이콘 보조
- **사운드 on/off** 전역 토글
- **읽기 부담 최소화** — 그림/아이콘 우선, 텍스트는 짧게
- **키보드/터치/마우스** 모두 자연스럽게 동작
- **오프라인 동작** (PWA, 기내·이동 중 사용 가능)

---

## 5. 기술 스택 (추천)

### 5.1 추천 스택
| 영역 | 선택 | 이유 |
|------|------|------|
| **프레임워크** | **Next.js 15 (App Router) + React 19 + TypeScript** | 확장성, PWA 지원, 추후 부모 대시보드 SSR 가능, 풍부한 생태계 |
| **스타일링** | **Tailwind CSS v4** | 빠른 프로토타이핑, 디자인 토큰 커스터마이징, 테마 전환 용이 |
| **UI 컴포넌트** | **shadcn/ui** + 커스텀 | 헤드리스 기반, 어린이용 디자인으로 변형 자유 |
| **애니메이션** | **Framer Motion** | 정답 이펙트, 캐릭터 모션, 페이지 전환 |
| **사운드** | **Howler.js** | 효과음 관리 (정답/오답/배경음악), 모바일 호환 |
| **상태관리** | **Zustand** | 가볍고 학습 곡선 낮음, 학습 진행/세션 상태 |
| **로컬 저장** | **IndexedDB (via `idb`)** | 학습 기록, 오프라인 캐시 |
| **i18n** | **next-intl** | App Router 친화, 토글 간단 |
| **테스트** | **Vitest + Playwright** | 단위/통합 + 브라우저 E2E |
| **배포** | **Vercel** | Next.js 무중단, 무료 티어로 충분 |

### 5.2 향후 추가 (필요 시)
| 영역 | 후보 | 도입 시점 |
|------|------|-----------|
| 백엔드 / 동기화 | **Supabase** (Postgres + Auth) | 디바이스 간 진행률 동기화 필요해질 때 |
| 분석 | **PostHog** (셀프호스트 또는 클라우드) | 사용 패턴 파악 |
| 에러 추적 | **Sentry** | 안정화 단계 |

### 5.3 디렉토리 구조 (제안)
```
math-tutor/
├── app/                     # Next.js App Router
│   ├── (game)/             # 게임 모드
│   ├── parent/             # 부모 대시보드
│   └── api/                # 필요 시 라우트
├── components/
│   ├── game/               # 게임 UI (몬스터, 우주선...)
│   ├── math/               # 문제 컴포넌트
│   └── ui/                 # 공통
├── lib/
│   ├── problems/           # 출제 엔진 (확장 포인트)
│   │   ├── multiplication.ts
│   │   ├── addition.ts
│   │   └── subtraction.ts
│   ├── progression/        # XP/레벨/보상 로직
│   ├── adaptive/           # 적응형 난이도
│   └── storage/            # IndexedDB 래퍼
├── themes/                 # 테마 (자산 + 텍스트)
│   ├── space/
│   └── monster/
├── messages/               # i18n (ko.json, en.json)
└── public/                 # 사운드, 이미지, 캐릭터 스프라이트
```

### 5.4 확장 포인트 (Extension Architecture)
새 연산을 추가할 때 건드릴 곳을 최소화:

```ts
// lib/problems/types.ts
export interface ProblemModule {
  id: string;                          // 'fraction-add'
  name: { ko: string; en: string };
  difficulties: DifficultyLevel[];
  generate: (config: GenerateConfig) => Problem;
  validate: (problem: Problem, answer: string) => Result;
  hint?: (problem: Problem, attemptCount: number) => string;
}
```
- 신규 모듈은 이 인터페이스만 구현하면 게임/테마/진행 시스템에 자동 통합
- 테마는 콘텐츠와 직교 — 어떤 테마에서도 어떤 연산이든 출제 가능

---

## 6. 데이터 모델 (요약)

```ts
type User = {
  id: string;
  name: string;
  avatar: AvatarConfig;
  level: number;
  xp: number;
  coins: number;
  selectedTheme: 'space' | 'monster' | ...;
  createdAt: Date;
};

type Session = {
  id: string;
  userId: string;
  startedAt: Date;
  endedAt?: Date;
  problemsAttempted: ProblemAttempt[];
};

type ProblemAttempt = {
  problemKey: string;          // e.g. 'mult:7x8'
  isCorrect: boolean;
  timeMs: number;
  attemptedAt: Date;
};

type Mastery = {
  problemKey: string;
  correctCount: number;
  totalCount: number;
  avgTimeMs: number;
  lastSeenAt: Date;
};

type Inventory = {
  collectibles: CollectibleId[];
  equippedItems: Record<EquipSlot, CollectibleId>;
};
```

---

## 7. 화면 구성 (Information Architecture)

```
[1] 첫 화면 (Home)
    - 오늘의 미션 카드 3개
    - "시작하기" CTA
    - 캐릭터 + 레벨 표시
    - 테마 전환 버튼 (👨‍🚀 ↔ ⚔️)

[2] 모드 선택
    - 곱셈 / 덧셈 / 뺄셈 / 보스전 / 약점 복습

[3] 난이도 선택
    - Easy / Normal / Hard / (조건부) Boss

[4] 게임 플레이
    - 상단: 진행도, 콤보, 시간(옵션)
    - 중앙: 문제 + 입력
    - 하단: 캐릭터/몬스터 (테마별)

[5] 결과 화면
    - 정답률, 획득 XP/코인, 잠금 해제 알림
    - "한판 더" / "홈으로"

[6] 도감/수집
    - 행성 지도 / 몬스터 도감 / 장비함

[7] 부모 모드 (PIN 진입)
    - 학습 통계, 약점, 설정
```

---

## 8. 로드맵 (점진적 확장 계획)

### Phase 1 — MVP (4-6주)
- 곱셈, 두자리수 덧셈/뺄셈
- 우주 + 몬스터 두 테마
- XP/레벨, 일일 미션, 수집 (간소판)
- 부모 모드 (기본 통계)
- 한/영 토글
- PWA 오프라인

### Phase 2 — 확장 (이후 4주)
- 나눗셈 (구구단 역산)
- 세자리수 연산
- 적응형 난이도 고도화
- 캐릭터 커스터마이징 강화
- 추가 테마 1개 (공룡 탐사대 권장)

### Phase 3 — 심화 (이후 6주)
- 분수 입문
- 시계 보기
- 단순 문장제 (스토리 문제)
- 도형 인지 (회전, 대칭)

### Phase 4 — 사회적 요소 (검토)
- 가족 내 캐릭터 비교 (경쟁 아닌 응원)
- 클라우드 동기화 (Supabase)
- 형제/친구와 비동기 챌린지

---

## 9. 성공 지표 (KPI)

| 지표 | 목표 (MVP 출시 후 1개월) |
|------|--------------------------|
| 일일 활성 사용 (아이) | 주 5회 이상 |
| 평균 세션 시간 | 8–15분 |
| 세션당 문제 수 | 20문제 이상 |
| 곱셈 정답률 | 시작 60% → 1개월 후 85%+ |
| 자발적 재개율 | 70%+ (부모 권유 없이 스스로 열기) |

> 정량 지표는 부수적이며, **"아이가 즐거워하는가"가 최우선** 판단 기준.

---

## 10. 비기능 요구사항

- **성능**: 첫 인터랙션까지 2초 이내 (PWA 캐시 후 1초)
- **오프라인**: 핵심 기능 100% 오프라인 동작
- **반응형**: 태블릿 가로/세로, 노트북, 데스크탑 (모바일 폰은 차순위)
- **개인정보**: 로컬 우선, 외부 전송 최소화. 클라우드 도입 시 명시적 동의
- **보안**: 부모 모드 PIN, 외부 링크 화이트리스트 (어린이 보호)

---

## 11. 위험 요소 & 완화

| 리스크 | 영향 | 완화 |
|--------|------|------|
| 게이미피케이션이 학습보다 우선시되어 도파민 루프화 | 학습 효과 저하, 과의존 | 일일 시간 한도, 미션 완료 후 자연스러운 종료 유도 |
| 한 테마에 금방 질림 | 사용 빈도 ↓ | 테마 회전, 이벤트 미션, 정기적 신규 테마 추가 |
| 너무 쉬워서 또는 너무 어려워서 흥미 상실 | 이탈 | 적응형 난이도, 부모 조정 옵션 |
| 오답으로 인한 좌절 | 자존감 저하 | 부드러운 피드백, 게임 오버 없음, 힌트 제공 |
| 확장 시 코드 비대화 | 유지보수 ↓ | 모듈 인터페이스 + 테마 직교성 설계 |

---

## 12. 오픈 이슈 (다음 결정 필요)

- [ ] MVP에서 캐릭터 커스터마이징 어디까지? (색상만 vs 아이템 시스템)
- [ ] 사운드 — 직접 녹음 vs 무료 라이센스 자산 ([Freesound](https://freesound.org/), [Mixkit](https://mixkit.co/))
- [ ] 초기 캐릭터/몬스터 일러스트 — AI 생성 vs 무료 자산 vs 의뢰
- [ ] 부모 모드 PIN을 같은 디바이스에서 어떻게 설정할 것인지 (초기 온보딩)
- [ ] 영어 모드의 음성 안내 — TTS vs 사전 녹음

---

## 13. 다음 단계 (Action Items)

1. PRD 리뷰 및 승인
2. 디자인 키 비주얼 (캐릭터 1체 + 몬스터 1체 + 우주선 1체) 결정
3. Next.js 프로젝트 스캐폴딩, Tailwind/Zustand/i18n 셋업
4. 출제 엔진 인터페이스 + 곱셈 모듈 먼저 구현 (수직 슬라이스)
5. 한 가지 테마 (우주) 풀스택 통합 → 다른 테마 추가
6. 가족 내부 사용 → 피드백 반영 → Phase 2 시작
